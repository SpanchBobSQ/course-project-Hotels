{
  "name": "stayfind-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static StayFind demo with Sass styles and XML hotel data.",
  "scripts": {
    "build:sass": "sass assets/scss/index.scss assets/css/index.css --no-source-map",
    "check:html": "node scripts/check-html.js",
    "lint": "npm run build:sass && npm run check:html"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
